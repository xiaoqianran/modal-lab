# Plan

输入固定选择 `040` 的 `sana-sprint-1.6b / seed 42`，保证 4 个 3D 模型比较同一输入。

实验同时记录 preprocess evidence：execution、engine、foreground ratio、component count、canonical digest。

3D 使用 capability 文档中每个模型的 `recommended` profile options，并固定 `seed=42`。
